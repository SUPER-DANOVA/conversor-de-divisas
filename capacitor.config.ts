import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.divisapp22011393.org',
  appName: 'DivisApp-22011393',
  webDir: 'dist'
};

export default config;
